Config { font = "xft:Monospace:pixelsize=12"
       , bgColor = "#151515"
       , fgColor = "#d0d0d0"
       , position = Top
       , lowerOnStart = True
       , commands = [ Run Date "%d/%m/%y %H:%M" "date" 10
                    , Run StdinReader
                    ]
       , sepChar = "%"
       , alignSep = "}{"
       , template = "%StdinReader% }%date%{"
       }
